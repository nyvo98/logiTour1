import { defaultModel } from '../common/constants'

export default {
  page: defaultModel.object,
  device: defaultModel.object,
  message: defaultModel.string,
  version: defaultModel.string,
  createdUser: defaultModel.string,
  isActive: defaultModel.boolean
}
