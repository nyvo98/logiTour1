import { defaultModel } from '../common/constants'

export default {
  type: defaultModel.string,
  bonusValue: defaultModel.array,
  image: defaultModel.string,
  listUserID: defaultModel.array,
  isActive: defaultModel.boolean
}
